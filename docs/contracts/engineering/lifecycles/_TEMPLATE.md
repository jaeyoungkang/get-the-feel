---
id: lifecycle:{slug}
title: {Human-readable lifecycle title}
status: draft
supports:
  - promise:{promise-slug}
---

# {Human-readable lifecycle title}

## States

- `{state}`

## Allowed Transitions

- `{from}` -> `{to}` when {condition}

## Forbidden Transitions

- `{from}` -> `{to}` because {risk}

## Evidence Expectations

- Describe the runtime, migration, scheduler, or audit evidence expected before this lifecycle can support a `met` verdict.
